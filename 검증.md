# SLOPE_EXIT wz 검증 (실제 경사로 전용, 정적 확인 불가)

```bash
ros2 launch robot_bringup autonomous_planc.launch.py
ros2 topic echo /slope_traverse/debug
```
(`/slope_traverse/debug` 발행 코드는 이미 있음 -- `slope_traverse_planc_node.cpp`의 `publishDebug()`. 새로 추가할 코드 없음, 위 명령으로 바로 확인 가능.)

-------------------------------------------------------------------

## 0. 제일 먼저 확인 — SLOPE_EXIT에 진입이나 하는가

지금까지의 가설(blend/lookahead/path 노이즈)은 전부 **"SLOPE_EXIT에 진입은 했다"는 전제** 위에 있음. 그런데 예전에 완전 개방루프(path/센서 전혀 안 보는 강제회전)로 테스트했을 때도 안 됐던 적이 있어서, 그보다 더 앞단 — **애초에 상태 전환 자체가 안 되거나, 명령이 실제로 액추에이터에 안 먹히는 것**일 가능성부터 배제해야 함.

**확인**: `/slope_traverse/debug` index 0(`state_enum`)이 실제로 **2(SLOPE_EXIT)**로 바뀌는지.

- **(A) state_enum이 계속 1(SLOPE_DRIVE)에 머물고 2로 안 바뀜**
  → 조향 로직과 무관, **트리거 자체가 안 걸림**. **최유력 원인**: `side`(entered_side_, index 1)가 `last_pitch_signed_deg_`(index 11)가 -5°까지 내려가기 **전에** 먼저 0으로 풀림 → `side_exit_dwell_sec_`(0.25초) 뒤 **IDLE로 튕겨나가서 pitch 트리거 자체가 확인될 기회를 잃음(state_enum이 1→0으로 바로 감, 2를 거치지 않음)**.
    - 근거: `side`는 인지팀 roll_deg(좌우 높이차) 기반이고 SLOPE_EXIT 트리거는 myAHRS+ pitch(전후 기울기) 기반 — **서로 독립적인 축**이라, "좌우 경사(roll)는 먼저 평평해지고 앞뒤 오르막(pitch)은 아직 안 끝난" 지형이면 roll 기반 `side`가 pitch보다 먼저 0으로 풀릴 수 있음(`slope_decision.py`가 `on_depth()`마다, ~15Hz로 계속 재발행 — 이벤트성 아님).
    - 확인: index 1(entered_side_)이 index 11(last_pitch_signed_deg_)이 -5°에 도달하기 **전에** 0으로 바뀌는지 시간순으로 비교. 이게 맞으면 blend/lookahead/pure pursuit 등 지금까지의 모든 조향 관련 시도가 애초에 실행될 기회조차 없었다는 뜻 — **개방루프(FORCED_TURN)까지 안 됐던 이유와도 정확히 들어맞음**(그것도 SLOPE_DRIVE case 안에서만 체크됨).
    - 이게 원인이면 해결책은 조향 로직이 아니라 **`side_exit_dwell_sec_`를 늘리거나, side==0이어도 pitch가 이미 임계값을 넘겼으면 IDLE로 안 빠지게 하는 예외 처리** 추가.
- **(B) state_enum은 2로 바뀌는데 로봇이 안 움직이거나 명령대로 안 움직임**
  → 상태머신은 정상, `/cmd_vel_safety` 우선순위 메커니즘 쪽 의심. `rmd_x8_driver_node`가 `cmd_vel_safety_timeout`(기본 0.5초) 이내 최신 수신 시 `/cmd_vel`(MPPI)을 무시하고 이걸 써야 하는데 실제로 그런지 확인 필요 — `ros2 topic echo /cmd_vel_safety`와 `ros2 topic echo /cmd_vel`을 동시에 띄워서 SLOPE_EXIT 구간에 어느 쪽 값이 실제로 모터에 반영되는지 비교.

**이 단계부터 확정해야 그 아래 가설들(blend/lookahead)이 의미가 있음.**

-------------------------------------------------------------------

**가설 1 (planC 자체 구조, 0번에서 (A) 아니고 SLOPE_EXIT 진입은 확인된 경우에 한해 유효)**: `yaw_err = (1-blend)*(entry_yaw_deg_-last_yaw_deg_) + blend*last_rel_offset_deg_`라서, blend<1이면 P제어가 "진입헤딩~path 타협점"에서 `yaw_err≈0`으로 정착 → `kp_exit_*yaw_err`가 작아서 400/150dps로 넓혀둔 `computeExitWzMax()`(≈1.0rad/s)까지 도달을 못 함. planA가 동일 증상 이미 문서화("blend 낮은 동안 yaw_err가 e0로 굳어버려 wz 상한 튜닝이 무의미") 하고 pure pursuit으로 교체한 전례 있음.

## debug 필드 (index)
| idx | 필드 | 용도 |
|---|---|---|
| 6 | wz_cmd | 실제 발행값 — 이론상 최대(≈1.0) 대비 확인 |
| 9 | pitch_filtered_deg_ | 8°↓=blend≈1, 22°↑=blend≈0, 사이=가설1 조건 성립 |
| 4 | yaw_err | 이미 작으면 dps 문제 아니라 path 인식 문제 |

**판정**: SLOPE_EXIT 중 `pitch_filtered_deg_`가 8~22° 구간 && `wz_cmd`≪1.0 && 로봇이 육안으로 안 돎 → 가설1 확정.

-------------------------------------------------------------------

## ⚠️ 중요: A(planC)/B(planB) 둘 다 "path 반대방향 조향"을 한 번씩 겪음(실측)

즉 **제어 방식(blend+P제어 vs pure pursuit) 차이가 원인이 아니라 `/path` 데이터 자체가 튀는 게 근본 원인**일 가능성이 높음. 그래서 지금 planC를 기준으로 잡고 있는 게 합리적 — 둘 다 같은 데이터를 받으면 같은 문제를 겪으니까.

**다음 확인 후보**: `/path` 발행 소스가 `slope_decision.py`에서 `/terrain/planned_path`(gradient_map, depth 기반)와 `/flatdrive/planned_path`(flat_drive, RGB 세그멘테이션 기반) 두 개 중 하나를 임계각 기준으로 골라서 나감 — **전환 순간** 서로 다른 센싱 방식 간 연속성이 없어서 방향이 뚝 끊길 수 있음. `/path`만 따로 기록해서 튀는 시점이 이 전환 시점과 겹치는지 확인 필요.

-------------------------------------------------------------------

## planZ(`autonomous.planz.py`) 대조군 — lookahead 거리 차이가 유력 후보

`ros2 launch robot_bringup autonomous.planz.launch.py use_outer_wheel_boost:=true target_linear_speed_m_s:=0.4`로 실측했을 때는 path를 잘 받아왔음(사용자 확인). 코드 구조는 planA/B의 `computeSlopeExitPursuitCurvature()`와 **거의 동일**(같은 lookahead 탐색 루프, 같은 fallback, 같은 `2y/L²` 공식) — ~~escort_follow 데드밴드/유예시간~~ 같은 방어 로직 없음(파일 docstring에 명시: "2026-08-27 시점, 가장 단순했던 pure pursuit"). **틀린 가설이었음, 폐기.**

**실제 차이는 lookahead 거리뿐으로 보임**:
- planZ 실행 시 `lookahead_distance_m` 오버라이드 안 함 → 기본값 **0.8m**
- planA/B의 `path_tangent_lookahead_m_` = **0.5m**

```cpp
curvature = 2.0 * y / (look_dist * look_dist)   // L이 분모 제곱 -- 짧을수록 y 노이즈 증폭
```
L이 짧을수록(0.5m) 같은 y 노이즈가 더 크게 증폭됨. 정상부 근처 카메라 시야 축소(짧은 path → 코앞 fallback점 사용)와 겹치면 A/B(0.5m)가 planZ(0.8m)보다 노이즈에 더 취약했을 가능성.

**다음 액션**: A/B의 `path_tangent_lookahead_m`을 0.8m로 올려서 재현되는지 실측 확인.

**TF 실패 처리 차이(재검토)**: planA는 TF 실패 시 `path_base_xy_.clear()`로 비움(→wz=0, 직진), planZ는 실패해도 이전 path 유지(→살짝 stale한 값 재사용). 둘 다 최악이 "0" 또는 "약간 늦은 값"이라 **부호 자체를 뒤집는 메커니즘은 아님** — 그래서 "반대 방향"의 직접 원인일 가능성은 낮음. 다만 **정상부 근처에서 TF lookup이 실제로 자주 실패한다면** planA는 "정상 곡률 ↔ 0"을 왔다갔다 해서 들쭉날쭉하게 보일 순 있음(반대 방향이라기보단 불안정). 확인: planA/B 로그에 `SLOPE_EXIT pure pursuit TF lookup ... 실패` 메시지가 정상부 근처에 자주 찍히는지 체크 — 찍히면 이것도 원인 후보로 격상, 안 찍히면 lookahead 가설에 더 무게.

(그 외 차이 — 우선순위 낮음: planZ는 `/motor_speed_cmd` 직접 발행/A·B는 Twist→rmd_x8_driver 변환. 상시 반전이라면 몰라도 "정상부 근처에서만" 튀는 증상과는 안 맞음.)

-------------------------------------------------------------------

## 해결 옵션 (원인 확정 후)

- **옵션 1 (lookahead 확대)**: `path_tangent_lookahead_m`(A/B 공용 파라미터)을 0.5→0.8m로. 재빌드 불필요(yaml만 수정).
- **옵션 2 (blend 강제 고정, planC 구조 유지)**: SLOPE_EXIT 진입 시 blend=1.0 고정. `kp_exit_`/dps 클램프 그대로 재사용.
- **옵션 3 (구조 변경, planB 방식)**: `kp_exit_`/blend 버리고 `wz=vx*kappa` pure pursuit로 교체.
- **path 소스 전환 문제로 확정되면**: slope_decision.py의 전환 로직 자체에 히스테리시스/디바운스 추가 필요 — 이건 인지팀 협의 필요.
