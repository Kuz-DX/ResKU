# [2026-08-30] target_detector_node.py 삭제(서플라이박스 인식은 dolbotz
# 패키지의 summer_supply로 통일 - 그쪽 requirements는 dolbotz/README나
# 별도 requirements에서 관리)로 ultralytics/openvino는 더 이상 필요 없어져
# 제거. 남은 건 side_view_visualizer.py(opencv-python)용 pip 의존성
# (rosdep 대상 아님).
# ROS Humble의 cv_bridge/matplotlib 바이너리는 NumPy 1.x ABI로 빌드되어 있어
# NumPy 2.x와 함께 로드하면 `_ARRAY_API not found`로 즉시 종료한다.
# 설치: python3 -m pip install --user -r requirements.txt
numpy>=1.24,<2
opencv-python
